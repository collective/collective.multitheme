<section class="pat-inject"
  tal:define="title item/Title; id view/id"
  id="fragment-${id}" tal:define="item view/load_item"
  data-anchor="anchor-${title}"
  title="${title}">
  <div id="pat-target-${id}">
    <a href="${view/item_url}"
      class="pat-inject" data-pat-inject="target: #pat-target-${id}; trigger: autoload; source: ${view/data/load_div}">Loading…</a>
  </div>
</section>
